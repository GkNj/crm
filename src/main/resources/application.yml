spring:
  datasource:
    username: root

    password: lilu19970327
    url: jdbc:mysql://localhost:3306/crm
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect


  thymeleaf:
    suffix: .html
    mode: HTML
